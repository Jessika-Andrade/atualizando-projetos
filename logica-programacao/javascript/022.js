/* Se preco = 100 e desconto = 20, qual expressão verifica se o preço final é estritamente menor que 80 */

let preco = 100;
let desconto = 20;
let valorFinal = (preco - desconto) < 80;

console.log(valorFinal)